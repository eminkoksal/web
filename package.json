{
  "name": "eminkoksal-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "eminkoksal.com — personal site for Emin Köksal (academic economist & AI consultant).",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir dist-server && node prerender.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
